spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/hong-nice/sp-config # git仓库地址, 保存其他项目的配置文件 https://github.com/你的个人路径/sp-config
          searchPaths: config

server:  port: 6001
  
eureka:  client:    service-url:      defaultZone: http://eureka1:2001/eureka, http://eureka2:2002/eureka


